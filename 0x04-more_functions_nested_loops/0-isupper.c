#include <stdio.h>
#include <ctype.h>
#include "main.h"

/**
 * _isupper -function check if a c is uppercase
 * @c: char to check
 * Return: 1 or 0
 */
int _isupper(int c)
{
	if (isupper(c))
		retturn (1);
	return (0);
}
